name: Voting

on:
  pull_request_target:
    types: [opened, reopened, closed]

jobs:
  democracy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Evaluate vote
        uses: myyk/git-democracy@v1
